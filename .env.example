# Gallery Manager Environment Configuration
# Copy this file to .env.local for local development

# Storage Provider: 'r2' (uses Worker API), 'imgur', 's3', 'gdrive'
VITE_STORAGE_PROVIDER=r2

# Worker API URL (for r2 storage provider)
# This is the Cloudflare Worker that handles R2 access securely
VITE_R2_API_URL=https://textsite-r2-api.matthewpereira.workers.dev

# Auth0 Configuration
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your_client_id
VITE_AUTH0_AUDIENCE=https://your-api-identifier/
VITE_AUTH0_REDIRECT_URI=http://localhost:5173

# Imgur Configuration (only needed if using 'imgur' storage provider)
# VITE_IMGUR_CLIENT_ID=your_imgur_client_id
# VITE_IMGUR_CLIENT_SECRET=your_imgur_client_secret
# VITE_IMGUR_REDIRECT_URI=http://localhost:5173
